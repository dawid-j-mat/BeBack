# BeBack – environment variables template.
# Copy to .env.local and fill in real values. Never commit .env.local.
# (No variables needed yet – Supabase keys arrive with slice 2,
# Google Places key with slice 3.)
